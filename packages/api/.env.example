PORT=4000
MONGO_URI=mongodb://localhost:27017/trellojunior
JWT_SECRET=change-me-in-production
CORS_ORIGIN=http://localhost:5173
# Only set to "true" for a deployment that actually terminates TLS - the auth
# cookie won't persist over plain HTTP if this is true (see src/lib/authCookie.ts).
COOKIE_SECURE=false
